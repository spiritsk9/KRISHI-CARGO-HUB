<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sample">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo9"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyFirebaseMessagingService" />
        <activity android:name=".UpdateSpace" />
        <activity android:name=".AcceptedLoad" />
        <activity android:name=".RequestList" />
        <activity android:name=".RequestPending" />
        <activity android:name=".Ridedetail" />
        <activity android:name=".DriverList" />
        <activity android:name=".AboutUs" />
        <activity android:name=".FeedBack" />
        <activity android:name=".FarmerProfile" />
        <activity android:name=".DriverProfile" />
        <activity
            android:name=".FarmerMapActivity"
            android:label="@string/title_activity_farmer_map" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Loaddetail" />
        <activity
            android:name=".DriverMapActivity"
            android:label="@string/title_activity_driver_map" />
        <activity android:name=".Fdashboard" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Fresetpassword" />
        <activity android:name=".ViewBillActivity" />
        <activity android:name=".Ddashboard" />
        <activity android:name=".Driversignup" />
        <activity android:name=".Farmersignup" />
        <activity android:name=".Driverlogin" />
        <activity android:name=".Farmerlogin" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>